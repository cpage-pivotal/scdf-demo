---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
- name: bubble

outputs:
- name: build-bubble-output

run:
  path: scdf-demo/ci/build-bubble.sh