---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: '8'

inputs:
- name: scdf-demo

outputs:
- name: run-regiser-apps-output

params:
  CF_API:
  CF_API2:

run:
  path: scdf-demo/ci/run-register-apps.sh